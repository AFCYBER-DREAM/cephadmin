---
# defaults file for cephadmin

packages_to_install:
  - ansible
  - ceph-ansible

rhcs_version: 2.3
rh_ceph_version: "{{ rhcs_version.split('.')[0] }}"

devices:
  - device: sda
    journal: nvme0n1
